export interface IndustryProject {
  id: number;
  projectKey: string;
  name: string;
}

export interface IndustryDevice {
  deviceKey: string;
  deviceSecret: string;
  id: number;
  name: number;
  productId: number;
  productName: string;
  status: number;
}
